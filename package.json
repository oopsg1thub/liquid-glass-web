{
  "name": "liquid-glass-web-repository-tools",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Private validation and GitHub Pages tooling for the liquid-glass-web skill",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:check": "node scripts/build.mjs --check",
    "validate:project": "node scripts/check-project.mjs",
    "validate:html": "html-validate \"skills/liquid-glass-web/references/example-*.html\" \".site-dist/**/*.html\"",
    "check": "npm run build:check && npm run validate:project && npm run validate:html",
    "serve": "python3 -m http.server 4173 -d .site-dist",
    "preview": "npm run build && node scripts/capture-preview.mjs",
    "test:browser": "playwright test",
    "test": "npm run build && npm run validate:project && npm run validate:html && npm run test:browser"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.12.1",
    "@playwright/test": "^1.62.1",
    "html-validate": "^11.6.0"
  }
}
